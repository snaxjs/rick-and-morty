import React from "react";
import { classNames } from "../../utils/ClassNames";

interface ICharPageProps {
  classNames?: string[];
}

const CharPage = (props: ICharPageProps) => {
  return <main className={classNames("char-page")}>asdasd</main>;
};

export default CharPage;
